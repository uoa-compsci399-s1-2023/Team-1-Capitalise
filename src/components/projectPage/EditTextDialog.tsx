

interface EditTextDialogProps {
  fieldName: string
  label: string
  isOpen: boolean,
  setIsOpen: React.Dispatch<React.SetStateAction<boolean>>
}


export default function TeamNameEditDialog({isOpen, setIsOpen, fieldName, label }: EditTextDialogProps) {

  return (

  );
}